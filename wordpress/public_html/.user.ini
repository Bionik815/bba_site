; Production (LiteSpeed/PHP-FPM) upload + memory limits for large catalog
; CSV imports and media sideloading. Local dev uses conf.d/zz-uploads.ini.
upload_max_filesize = 32M
post_max_size = 48M
memory_limit = 512M
max_execution_time = 300
max_input_time = 300
